name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  python:
    name: Python (${{ matrix.python-version }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12", "3.13"]
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          python-version: ${{ matrix.python-version }}
      - name: Install package
        run: uv sync --extra dev
      - name: Lint with ruff
        run: uv run ruff check .
      - name: Check formatting
        run: uv run ruff format --check .
      - name: Run pytest
        run: uv run pytest --cov=bottom_50_tax_analysis --cov-report=term-missing
      - name: Generate fallback results.json
        run: uv run bottom50-generate --output data/results.json

  frontend:
    name: Frontend build
    runs-on: ubuntu-latest
    needs: python
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
        with:
          enable-cache: true
          python-version: "3.13"
      - name: Install Python package (for data generation)
        run: uv sync
      - name: Regenerate frontend data (fallback mode)
        run: |
          uv run bottom50-generate --output data/results.json
          cp data/results.json frontend/data/results.json
      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
      - name: Install frontend deps
        working-directory: frontend
        run: bun install --frozen-lockfile
      - name: Lint frontend
        working-directory: frontend
        run: bun run lint
      - name: Build frontend
        working-directory: frontend
        run: bun run build
